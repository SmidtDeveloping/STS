<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beheer bank</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;500&display=swap">
    <style>
    body {
        background-color: #212529; /* Bootstrap dark background color */
        color: #fff; /* Bootstrap light text color */
        font-family: "Lexend", sans-serif;
    }

    .container {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        max-width: 400px; /* Adjusted max-width for better responsiveness */
    }
</style>
</head>
<body class="bg-dark text-white d-flex align-items-center justify-content-center" style="height: 100vh;"> <!-- Bootstrap flexbox klassen gebruikt om te centreren -->
    <div class="container">
        <p>Lege values worden <strong>NIET</strong> geupdate deze blijven hetzelfde</p>
        <form action="/dashboard/punten/beheer?userid=<%= bank.userId %>" method="post">
            <div class="mb-3">
                <label for="punt" class="form-label">Punten:</label>
                <input type="number" class="form-control" id="punt" name="punt">
            </div>
            <div class="mb-3">
                <label for="woord" class="form-label">Woorden geraden:</hlabel>
                <input type="number" class="form-control" id="woord" name="woord">
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </div>
    <!-- Bootstrap JS (optioneel, als je JavaScript-functies van Bootstrap wilt gebruiken) -->
    <!-- <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script> -->
</body>
</html>
