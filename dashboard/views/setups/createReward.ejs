<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configuratie Formulier</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;500&display=swap">
    <style>
        body {
            background-color: #212529;
            color: #fff;
            font-family: 'Lexend', sans-serif; /* Lexend als het gewenste lettertype */
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #343a40;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            display: flex; /* Gebruik flexbox om elementen naast elkaar te plaatsen */
        }

        .list {
            flex: 1; /* Laat de lijst groeien om beschikbare ruimte in te nemen */
            margin-right: 20px; /* Ruimte tussen de lijst en het formulier */
        }

        form {
            width: 50%; /* Breedte van het formulier */
        }

        .mb-3 label {
            color: #adb5bd;
        }

        .form-select,
        .form-control {
            background-color: #454d55;
            color: #fff;
            border: none;
        }

        .form-select:focus,
        .form-control:focus {
            background-color: #495057;
            color: #fff;
            border-color: #ced4da;
            box-shadow: none;
        }

        .btn-primary,
        .btn-secondary {
            background-color: #007bff;
            border-color: #007bff;
        }

        .btn-primary:hover,
        .btn-secondary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="list">
            <h1>Configuratie Formulier</h1>
            <p>Role setup:</p>
            <ul>
                <% data.roloes.forEach(role => { %>
                    <li>Role: <%= role.roleName %></li>
                    <li>Reward: <%= role.reward  %></li>
                    <br>
                <% }); %>
            </ul>
            <a href="/dashboard" class="btn btn-primary"">Terug naar dashboard</a>
        </div>

        <form action="/dashboard/create-reward" method="POST">      
            <div class="mb-3">
                <label for="rol" class="form-label">Selecteer een rol:</label>
                <select id="rol" name="role" class="form-select">
                    <% data.roles.forEach(role => { %>
                        <option value="<%= role.name %>"><%= role.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="mb-3">
                <label for="value" class="form-label">€:</label>
                <input type="text" id="value" name="value" required class="form-control">
            </div>
            <button type="submit" class="btn btn-outline-primary">Opslaan</button>
        </form>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
