<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Selector</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">    
    <style>
        body {
            background-color: #343a40; /* Dark background color */
            color: #f8f9fa; /* Light text color */
        }
        .card {
            background-color: #495057; /* Card background color */
            border: none; /* Remove card border */
            color: #f8f9fa; /* Card text color */
        }
        .card-title {
            color: #f8f9fa; /* Card title text color */
        }
        .card-title a {
            color: #0d6efd; /* Link color */
        }
        .card-title a:hover {
            color: #0d6efd; /* Hover link color */
            text-decoration: none; /* Remove underline on hover */
        }
        .card-img-top {
            width: 100px;
            height: 100px;
            object-fit: cover;
            border-radius: 50%;
            margin-top: -50px; /* Adjust top margin */
            border: 2px solid #f8f9fa; /* Border color for the image */
        }
        .btn-primary {
            background-color: #0d6efd; /* Primary button background color */
            border-color: #0d6efd; /* Primary button border color */
        }
        .btn-primary:hover {
            background-color: #0b5ed7; /* Primary button hover background color */
            border-color: #0b5ed7; /* Primary button hover border color */
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row">
            <% guilds.forEach(guild => { %>
                <div class="col-md-4 mb-4">
                    <div class="card text-center">
                        <div class="card-body">
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="<%= guild.name %> icon" class="card-img-top mx-auto">
                            <h5 class="card-title mt-3"><%= guild.name %></h5>
                            <form action="/servers" method="post">
                                <input type="hidden" name="guildId" value="<%= guild.id %>">
                                <button type="submit" class="btn btn-primary mt-3">Beheer</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-HAE54VxgbbgkzCYtbj2yS0G8YP4/NLM+F4ltq3+2CZ86v0KRTnW4+gQO7jgMd4+T" crossorigin="anonymous"></script>
</body>
</html>
